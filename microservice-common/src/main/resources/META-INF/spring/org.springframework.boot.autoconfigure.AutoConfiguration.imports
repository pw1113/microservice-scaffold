# Jackson 序列化配置 —— 统一 LocalDateTime/LocalDate 格式化、时区等
com.microservice.common.config.JacksonConfig

# WebMvc 配置 —— 注册用户上下文拦截器、配置全局跨域
com.microservice.common.config.WebMvcConfig

# SpringDoc (OpenAPI 3.x) 配置 —— 生成 Swagger 接口文档（需要 springdoc 依赖）
com.microservice.common.config.SpringDocConfig

# Redis 配置 —— 自定义 RedisTemplate 序列化方式，兼容 JavaTimeModule（需要 redis 依赖）
com.microservice.common.config.RedisConfig

# MyBatis-Plus 配置 —— 分页插件、乐观锁、自动填充 createTime/updateTime（需要 mybatis-plus 依赖）
com.microservice.common.config.MyBatisPlusConfig

# 枚举 Swagger 文档转换器 —— 自动为带有 @JsonValue 的枚举生成 code + description 文档（需要 swagger 依赖）
com.microservice.common.config.EnumModelConverterConfig

# OpenFeign 配置 —— 透传 TraceId 和用户上下文 Header 到下游服务（需要 feign 依赖）
com.microservice.common.config.FeignConfig
